from datetime import datetime

def reset_week():
    # Thực hiện logic reset tuần ở đây (ví dụ: reset week_point, lưu lịch sử tuần, ...)
    print(f"Reset weekly at {datetime.utcnow()}")

if __name__ == "__main__":
    reset_week()