from datetime import datetime

def reset_month():
    # Thực hiện logic reset tháng ở đây (ví dụ: reset month_point, lưu lịch sử tháng, ...)
    print(f"Reset monthly at {datetime.utcnow()}")

if __name__ == "__main__":
    reset_month()