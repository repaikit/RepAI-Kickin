from datetime import datetime

def reset_day():
    # Thực hiện logic reset ngày ở đây (ví dụ: reset daily_point, lưu lịch sử ngày, ...)
    print(f"Reset daily at {datetime.utcnow()}")

if __name__ == "__main__":
    reset_day()